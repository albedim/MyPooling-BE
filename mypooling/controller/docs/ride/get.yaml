Get rides of trip
---
tags:
  - Ride
parameters:
  - in: query
    name: tripId
    description: The param should contain the trip id
    required: true
  - in: query
    name: stepId
    description: The param should contain the step id
    required: true

responses:
  200:
    description: OK
  400:
    description: Bad request
